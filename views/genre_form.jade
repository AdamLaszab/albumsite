extends layout


block content 
    h1 #{title}
    form(method="POST")
      div
      label(for='name') name:
        input(type='text', name='name',required)
        button(type='submit') Submit

    if errors
        ul
            for error in errors
                li!= error.msg


